name: taskatti
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gap: ^3.0.1
  lottie: ^3.1.0
  google_fonts: ^6.2.1
  image_picker: ^1.0.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0


dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets: 
    - Assets/

